import React from "react";
import logo from "./logo.svg";

const logoImg = {logo};

const Logo = () => (

  <div className="logo">

    <img src={logoImg} alt="quantumone-logo" />

  </div>

);

export default Logo;
